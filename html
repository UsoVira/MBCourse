<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Інтерактивний план навчання з англійської мови</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Chosen Palette: Warm Neutral Harmony -->
    <!-- Application Structure Plan: The application uses a tab-based navigation structure to separate "Lexical" and "Grammar" topics, which is intuitive for the user. The core content is displayed as a grid of interactive cards. This approach allows for easy scanning of topics. Clicking a card opens a modal with details, preventing clutter on the main screen. A key feature is the progress tracking system (status buttons on cards and a summary donut chart), which gamifies the learning process and provides clear visual feedback. This structure was chosen to transform a static list into a dynamic and engaging tool for managing the learning journey. -->
    <!-- Visualization & Content Choices: Report Info: List of lexical and grammar topics. -> Goal: Organize, inform, and track progress. -> Viz/Presentation Method: Interactive cards for topics (HTML/Tailwind), a donut chart for overall progress (Chart.js), and a modal for detailed information (HTML/JS). -> Interaction: Users can filter topics by category, view details by clicking cards, and update the status of each topic. The chart updates dynamically with each status change. -> Justification: This combination of elements provides a clear, organized, and motivating user experience. It avoids static data presentation in favor of an interactive dashboard. -> Library/Method: Chart.js for data visualization, Tailwind CSS for styling, Vanilla JS for logic. -->
    <!-- CONFIRMATION: NO SVG graphics used. NO Mermaid JS used. -->
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        .chart-container {
            position: relative;
            width: 100%;
            max-width: 320px;
            margin-left: auto;
            margin-right: auto;
            height: 320px;
        }
        .nav-button-active {
            background-color: #3b82f6;
            color: white;
        }
        .nav-button-inactive {
            background-color: #e5e7eb;
            color: #374151;
        }
        .status-btn {
            transition: all 0.2s ease-in-out;
        }
        .status-btn:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body class="bg-stone-50 text-stone-800">

    <div class="container mx-auto p-4 sm:p-6 md:p-8">
        <header class="text-center mb-8">
            <h1 class="text-3xl md:text-4xl font-bold text-blue-600">Навчальний план з англійської мови</h1>
            <p class="mt-2 text-stone-600 max-w-2xl mx-auto">Це інтерактивний інструмент для відстеження прогресу у вивченні тем для 7-го класу. Оберіть категорію, щоб переглянути теми, та відмічайте їхній статус, щоб побачити свій прогрес на діаграмі.</p>
        </header>

        <main class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="lg:col-span-2">
                <nav class="flex justify-center gap-4 mb-6">
                    <button id="lexical-nav" class="nav-button-active text-white font-semibold py-2 px-6 rounded-lg shadow-md transition-colors duration-300">
                        📚 Лексика
                    </button>
                    <button id="grammar-nav" class="nav-button-inactive text-gray-700 font-semibold py-2 px-6 rounded-lg shadow-md transition-colors duration-300">
                        ✍️ Граматика
                    </button>
                </nav>
                <div id="topics-grid" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">
                </div>
            </div>

            <aside class="lg:col-span-1 bg-white p-6 rounded-xl shadow-lg">
                <h2 class="text-2xl font-bold text-center mb-4">Ваш прогрес</h2>
                <div class="chart-container">
                    <canvas id="progressChart"></canvas>
                </div>
                <div id="stats" class="text-center mt-4 space-y-2">
                </div>
            </aside>
        </main>
    </div>

    <div id="topic-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 hidden z-50">
        <div class="bg-white rounded-lg shadow-2xl p-6 w-full max-w-lg relative animate-fade-in">
            <button id="close-modal" class="absolute top-4 right-4 text-gray-500 hover:text-gray-800 text-2xl">&times;</button>
            <h3 id="modal-title" class="text-2xl font-bold mb-4 text-blue-600"></h3>
            <p id="modal-description" class="text-stone-600 mb-4">
                Тут буде детальний опис теми, ключові моменти для вивчення, приклади та корисні поради. Цей розділ допоможе краще зрозуміти, на що звернути увагу під час опрацювання матеріалу.
            </p>
             <div class="mt-6">
                <h4 class="font-semibold mb-2">Приклади речень:</h4>
                <ul id="modal-examples" class="list-disc list-inside text-stone-600 space-y-1">
                    <li>Приклад речення 1.</li>
                    <li>Приклад речення 2.</li>
                </ul>
            </div>
        </div>
    </div>


    <script>
        const data = {
            lexical: [
                { id: 'l1', title: 'Знайомство та сім\'я', status: 'todo' },
                { id: 'l2', title: 'Школа та шкільне життя', status: 'todo' },
                { id: 'l3', title: 'Хобі та дозвілля', status: 'todo' },
                { id: 'l4', title: 'Їжа та напої', status: 'todo' },
                { id: 'l5', title: 'Місто та визначні місця', status: 'todo' },
                { id: 'l6', title: 'Зовнішність та характер', status: 'todo' },
                { id: 'l7', title: 'Пори року та погода', status: 'todo' },
                { id: 'l8', title: 'Рутинні справи', status: 'todo' },
            ],
            grammar: [
                { id: 'g1', title: 'Дієслово to be', status: 'todo' },
                { id: 'g2', title: 'Present Simple', status: 'todo' },
                { id: 'g3', title: 'Present Continuous', status: 'todo' },
                { id: 'g4', title: 'Past Simple', status: 'todo' },
                { id: 'g5', title: 'Множина іменників', status: 'todo' },
                { id: 'g6', title: 'Артиклі a/an, the', status: 'todo' },
                { id: 'g7', title: 'Прийменники місця та часу', status: 'todo' },
                { id: 'g8', title: 'Модальні дієслова can/must', status: 'todo' },
                { id: 'g9', title: 'Ступені порівняння прикметників', status: 'todo' },
            ]
        };

        let currentCategory = 'lexical';
        let progressChart;

        const topicsGrid = document.getElementById('topics-grid');
        const lexicalNav = document.getElementById('lexical-nav');
        const grammarNav = document.getElementById('grammar-nav');
        const statsDiv = document.getElementById('stats');
        
        const modal = document.getElementById('topic-modal');
        const closeModalBtn = document.getElementById('close-modal');
        const modalTitle = document.getElementById('modal-title');

        const statusConfig = {
            todo: { text: 'Почати', color: 'bg-gray-200', textColor: 'text-gray-700', next: 'doing' },
            doing: { text: 'В процесі', color: 'bg-yellow-400', textColor: 'text-white', next: 'done' },
            done: { text: 'Вивчено', color: 'bg-green-500', textColor: 'text-white', next: 'todo' }
        };

        function renderCards() {
            topicsGrid.innerHTML = '';
            const topics = data[currentCategory];
            topics.forEach(topic => {
                const card = document.createElement('div');
                card.className = 'bg-white p-4 rounded-lg shadow-md flex flex-col justify-between cursor-pointer transition-transform hover:scale-105';
                card.addEventListener('click', (e) => {
                    if (!e.target.classList.contains('status-btn')) {
                        openModal(topic);
                    }
                });

                const status = statusConfig[topic.status];

                card.innerHTML = `
                    <h3 class="font-semibold text-lg mb-4 text-stone-800">${topic.title}</h3>
                    <button 
                        data-id="${topic.id}" 
                        class="status-btn w-full mt-auto py-2 px-4 rounded-md font-semibold ${status.color} ${status.textColor}">
                        ${status.text}
                    </button>
                `;
                topicsGrid.appendChild(card);
            });
        }

        function updateProgress() {
            const allTopics = [...data.lexical, ...data.grammar];
            const counts = {
                done: allTopics.filter(t => t.status === 'done').length,
                doing: allTopics.filter(t => t.status === 'doing').length,
                todo: allTopics.filter(t => t.status === 'todo').length,
            };
            const total = allTopics.length;

            progressChart.data.datasets[0].data = [counts.done, counts.doing, counts.todo];
            progressChart.update();
            
            statsDiv.innerHTML = `
                <p><span class="font-bold text-green-600">${counts.done}</span> з ${total} тем вивчено</p>
                <p><span class="font-bold text-yellow-500">${counts.doing}</span> тем в процесі</p>
                <p><span class="font-bold text-gray-500">${counts.todo}</span> тем очікує</p>
            `;
        }

        function handleNavClick(category) {
            currentCategory = category;
            lexicalNav.classList.toggle('nav-button-active', category === 'lexical');
            lexicalNav.classList.toggle('nav-button-inactive', category !== 'lexical');
            grammarNav.classList.toggle('nav-button-active', category === 'grammar');
            grammarNav.classList.toggle('nav-button-inactive', category !== 'grammar');
            renderCards();
        }
        
        function openModal(topic) {
            modalTitle.textContent = topic.title;
            modal.classList.remove('hidden');
        }

        function closeModal() {
            modal.classList.add('hidden');
        }

        function initChart() {
            const ctx = document.getElementById('progressChart').getContext('2d');
            progressChart = new Chart(ctx, {
                type: 'doughnut',
                data: {
                    labels: ['Вивчено', 'В процесі', 'Очікує'],
                    datasets: [{
                        data: [0, 0, data.lexical.length + data.grammar.length],
                        backgroundColor: [
                            '#22c55e',
                            '#f59e0b',
                            '#e5e7eb'
                        ],
                        borderColor: '#ffffff',
                        borderWidth: 4,
                        hoverOffset: 8
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    cutout: '70%',
                    plugins: {
                        legend: {
                            display: false
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    let label = context.label || '';
                                    if (label) {
                                        label += ': ';
                                    }
                                    if (context.parsed !== null) {
                                        label += context.parsed;
                                    }
                                    return label;
                                }
                            }
                        }
                    }
                }
            });
        }

        lexicalNav.addEventListener('click', () => handleNavClick('lexical'));
        grammarNav.addEventListener('click', () => handleNavClick('grammar'));
        
        closeModalBtn.addEventListener('click', closeModal);
        modal.addEventListener('click', (e) => {
            if (e.target === modal) {
                closeModal();
            }
        });

        topicsGrid.addEventListener('click', (e) => {
            if (e.target.matches('.status-btn')) {
                const topicId = e.target.dataset.id;
                const topics = data[currentCategory];
                const topic = topics.find(t => t.id === topicId);
                if (topic) {
                    topic.status = statusConfig[topic.status].next;
                    renderCards();
                    updateProgress();
                }
            }
        });

        document.addEventListener('DOMContentLoaded', () => {
            initChart();
            handleNavClick('lexical');
            updateProgress();
        });
    </script>
</body>
</html>
